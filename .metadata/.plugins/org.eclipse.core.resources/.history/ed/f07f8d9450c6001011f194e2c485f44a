#include <hal_general.h>
#include <dl_General.h>
#include "hal_usciB1.h"

#include <stdint.h>  // for uint32_t or other variables

#define BYTE1 'AA'
#define BYTE2 'CC'

extern USCIB1_SPICom LCD;
extern int one_hz_cnt;

// '0' ... motor init off
// '1' ... motor init on
int activate_motor = 0;

void main(void)
{
    LCD.Status.TxSuc = 1;
    hal_init();
    dl_Init();

    dl_LCDWriteText("A", 1, 1, 0);
    dl_LCDWriteText("B", 1, 1, 5);
    dl_LCDWriteText("C", 1, 2, 11);
    dl_LCDWriteText("D", 1, 2, 16);
    dl_LCDWriteText("E", 1, 3, 22);
    dl_LCDWriteText("F", 1, 3, 27);
//    dl_LCDWriteText("G", 1, 1, 6);
//    dl_LCDWriteText("H", 1, 1, 7);

    while (1)
    {
        //test_movements();

    }
}

