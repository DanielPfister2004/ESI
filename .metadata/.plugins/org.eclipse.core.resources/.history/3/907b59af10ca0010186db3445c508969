#include <hal_general.h>
#include <dl_General.h>
#include "hal_usciB1.h"
#include <string.h>


#include <stdint.h>  // for uint32_t or other variables

#define BYTE1 'AA'
#define BYTE2 'CC'

extern USCIB1_SPICom LCD;
extern int one_hz_cnt;

// '0' ... motor init off
// '1' ... motor init on
int activate_motor = 1;

void main(void)
{
    LCD.Status.TxSuc = 1;
    hal_init();
    dl_Init();

    test_message();

    while (1)
    {
        //test_movements();
        char buffer[10];
        sprintf(buffer, "%u", TA0CCR2);
        dl_LCDWriteText(buffer, strlen(buffer), 0, 0);
    }
}

